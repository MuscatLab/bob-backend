server:
  port: 8080
  profiles:
    active: dev

spring:
  datasource:
    url: jdbc:postgresql://db.mozza.pro/postgres
    username: postgres
    password: BDp5F7rGBYcNtYHT7NAxb5
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_batch_fetch_size: 1000
    open-in-view: false
  error:
    include-exception: false
    include-message: always
    inclue-stacktrace: on_param
    whitelabel:
      enabled: true
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

springdoc:
  groups: true
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.htmls

management:
  endpoints:
    web:
      exposure:
        include: prometheus